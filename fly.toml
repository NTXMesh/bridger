app = "ntxmesh-collector"
primary_region = "dfw"

[build]

[env]
  MQTT_PORT = "8883"
  MQTT_TOPIC = "msh/#"

# No [[services]] block — this is outbound-only, no inbound ports needed
# Omitting services prevents Fly health check failures

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
